'use strict';

const Mail = require('./mail');
const Message = require('./message');
const ResponseError = require('./response-error');

module.exports = {
  // Attachment,
  Mail,
  Message,
  ResponseError,
};